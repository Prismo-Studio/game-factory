# Post-launch — plus tard

Lit les stats stores / AdMob (export CSV déposé par un humain dans une Release `stats-<date>`, pas d'API tant qu'il n'y a pas de compte), en tire des tickets d'amélioration `triage` + `origin:post-launch`, et propose des A/B tests sur `game/data/`. À écrire quand un premier jeu est publié.
